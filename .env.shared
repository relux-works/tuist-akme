# Repo-tracked configuration
#
# These values define the canonical app/workspace identity for all developers and CI.
# Local-only overrides (team ID, bundle ID suffix) live in `.env` and are NOT committed.
#
# Values from this file are mapped to `TUIST_*` env vars by the `Makefile` so Tuist manifests
# can read them via `Environment.*`.

# Workspace
WORKSPACE_NAME=AcmeApp

# Identifiers
#
# Core reverse-DNS root used to derive all bundle IDs (apps, modules).
CORE_ROOT=com.acme.akmeapp
#
# Explicit shared identifier root (opt-in) used for cross-platform capability identifiers.
SHARED_ROOT=com.acme.akmeapp.shared

# iOS host app
IOS_APP_PROJECT_NAME=iOSApp
IOS_APP_NAME=AcmeApp
IOS_MIN_VERSION=16.0

# macOS host app
MACOS_APP_PROJECT_NAME=macOSApp
MACOS_APP_NAME=AcmeMacApp
MACOS_MIN_VERSION=13.0
